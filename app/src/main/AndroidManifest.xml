<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hesnelmoslem">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo1"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo1"
        android:supportsRtl="false"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.soundActivity.SoundActivity"
            android:parentActivityName=".ui.ZikerActivity.ZikerActivity">

        </activity>
        <activity android:name=".ui.ZikerActivity.ZikerActivity"
            android:parentActivityName=".ui.MenuActivity.MenuActivity"/>
        <activity android:name=".ui.MenuActivity.MenuActivity"
            >

        </activity>
        <activity android:name=".ui.MainActivityUi.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>